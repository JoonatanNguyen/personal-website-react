import React from 'react';

const Header = () => (
	<div className="navigation-bar">
		<span>About</span>
		<span>Portfolio</span>
		<span>Resume</span>
		<span>Contact</span>
	</div>
);

export default Header;
